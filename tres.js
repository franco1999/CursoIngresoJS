function Mostrar()
{
	var numeroUno;
	var numeroDos;
	var resultado;

	numeroUno = prompt("ingrese un numero");
	numeroUno = parseInt(numeroUno)

	numeroDos = prompt("ingrese un numero")
	numeroDos = parseInt(numeroDos);

	if(numeroUno + numeroDos) {
		document.write(numeroUno + numeroUno)
	}
	else if {
		(numeroUno * numeroDos)
	}
	else
		(numeroDos - numeroUno)
	




 

 // ingresar dos numeros, si son igusales hay que concatenarlos,
 // si el primero es mayor que el segundo multiplicarlo, y si el
 // primero es menor que el segundo hay que restarlo
}
