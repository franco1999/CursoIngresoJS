function Mostrar()
{

	var contador=0,numero;
	var respuesta="si"
	var maximo
	var minimo
	var numero 

	while(respuesta!="no")
	{
		numero=prompt("ingrese numero")
		numero=parseInt(numero);

	while(isNaN(numero))
	
	{
		numero=prompt("ingrese numero")
		numero=parseInt(numero)
	}
	if(bandera==0)
	{
		bandera++;
		max=numero;
		minimo=numero
	}
	else
	{
		if(max<numero)
	}
	else if (minimo<numero)
		minimo=numero;




}//FIN DE LA FUNCIÓN